//
// Created by jglrxavpok on 27/10/2022.
//

#include "GLTFLoader.h"

namespace Carrot::Render {
    LoadedScene GLTFLoader::load(const IO::Resource& resource) {
        LoadedScene result;

        TODO; // use tinygltf to load scene

        return std::move(result);
    }
}
